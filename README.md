# Проект: Пинг-Понг на Pygame

Этот учебный проект представляет собой классическую игру "Пинг-Понг", реализованную на языке Python. Он отлично подходит для изучения основ объектно-ориентированного программирования (ООП) и разработки простых игр.

## 1. Структура проекта
Для удобства работы проект разбит на несколько модулей:
- **`main.py`** — точка входа. Здесь находится основной игровой цикл, обработка событий и отрисовка.
- **`settings.py`** — хранилище всех настроек: цвета, размеры окна, частота кадров (FPS) и привязки клавиш.
- **`interface.py`** — отвечает за отображение счета игроков на экране.
- **`assets/`** — архитектурное сердце проекта. Здесь хранятся все визуальные и физические компоненты игры. Разделение на "ассеты" позволило нам не загромождать `main.py` и сделать код расширяемым.
  - **`__init__.py`** — этот файл превращает папку в Python-пакет. Без него мы не смогли бы делать красивые импорты вида `from assets.walls import P1`. Это стандарт профессиональной разработки: разделять логику на модули и пакеты.
  - **`game_sprite.py` (Базовый кирпичик)**:
    - *Для чего создан*: Чтобы соблюсти принцип **DRY** (Don't Repeat Yourself). У любого объекта (ракетки, мяча, стены) есть общие черты: координаты, размеры, скорость и метод отрисовки.
    - *Как создавался*: Мы создали класс-родитель `GameSprite`, наследуясь от `pygame.sprite.Sprite`. В инициализаторе (`__init__`) мы сразу создаем объект `pygame.Rect` — это "умный" прямоугольник, который Pygame использует для обработки столкновений и перемещения. В методе `update` мы заложили двойную отрисовку: сначала заливка тела объекта (`self.color`), а затем рисование рамки (`self.border_color`), что придает игре единый графический стиль.
  - **`walls.py` (Интеллект игрока)**:
    - *Для чего создан*: Для описания поведения ракеток, которыми управляют люди.
    - *Как создавался*: Класс `Walls` наследует всё от `GameSprite`. Нам не пришлось заново писать код отрисовки — мы просто расширили его. В методе `update` мы реализовали систему "подписки" на клавиши через `keybinds`. Это позволяет одной и той же логике работать как для левого игрока (W/S), так и для правого (Стрелки).
    - *Критическая деталь*: Мы добавили проверку границ окна (`self.rect.top > 0` и `self.rect.bottom < HEIGHT`). Это пример того, как программист ограничивает "свободу" объекта, чтобы он не "улетел" в бесконечность, следуя правилам игровой физики.
  - **`ball.py` (Автоная сущность)**:
    - *Для чего создаётся*: Мяч — это единственный объект, который должен "жить своей жизнью".
    - *Как он будет работать*: В отличие от ракеток, мяч в своём `update` должен будет изменять свои координаты не по нажатию клавиш, а автоматически на основе векторов скорости. Это заготовка для реализации физики отскоков и условий гола. Студенту предстоит реализовать здесь алгоритм отражения угла (`angle of incidence = angle of reflection`).

## 2. Ключевые этапы разработки (Как это сделано)

### Шаг 1: Базовый спрайт (`GameSprite`)
Мы создали универсальный класс для любого объекта на экране. Он наследуется от `pygame.sprite.Sprite` и умеет:
- Хранить позицию и размер в объекте `Rect`.
- Рисовать себя (прямоугольник) и свою рамку.

### Шаг 2: Ракетки и управление (`Walls`)
Класс ракеток расширяет базовый спрайт:
- В методе `update` мы проверяем, какие клавиши были нажаты.
- Мы добавили проверку границ экрана: если `rect.top` меньше 0 или `rect.bottom` больше высоты окна, движение блокируется.
- Ракетки создаются в двух экземплярах (`P1` и `P2`) с разными начальными позициями.

### Шаг 3: Интерфейс и логика счета (`Interface`)
- Используется модуль `pygame.font` для рендеринга текста.
- Счет хранится в классовых переменных и обновляется с помощью статических методов.
- Счет выводится в верхней части экрана для обоих игроков.

### Шаг 4: Основной игровой цикл (`main.py`)
- **Очистка**: Каждый кадр мы заливаем экран цветом `CYAN`.
- **Логика**: Вызываем методы `update` у интерфейса и ракеток.
- **Отрисовка**: `pygame.display.update()` обновляет окно, а `clock.tick(FPS)` гарантирует стабильную скорость игры.

## 3. Инструкция: что со всем этим делать?
1. **Запуск**: Установи `pygame` через `pip install pygame` и запусти `python main.py`.
2. **Изучение**: Посмотри, как в `walls.py` реализовано движение. Попробуй изменить скорость (`speed`) в `GameSprite`.
3. **Доработка**: Твоя главная задача — оживить файл `assets/ball.py`. Мяч должен летать, отталкиваться от стен и ракеток.

---
*Удачи в кодинге!* 🏓
